Data Analytics II 
1. Implement logistic regression using Python/R to perform classification on Social_Network_Ads.csv dataset. 
2. Compute Confusion matrix to find TP, FP, TN, FN, Accuracy, Error rate, Precision, Recall on the given dataset.

 TP=true positive ....value true and model gives true
 
 FP=false positive....value false but model give true

import pandas as pd
from sklearn.model_selection import train_test_split
from sklearn.linear_model import LogisticRegression
from sklearn.metrics import confusion_matrix,accuracy_score,precision_score,recall_score,mean_squared_error
import matplotlib.pyplot as plt 
import seaborn as sns

df=pd.read_csv(r"C:\Users\DELL\Desktop\dsbdal\Social_Network_Ads.csv")
df["Gender"].replace({'Male':0,'Female':1},inplace=True)    # done bcoz model trained only on numerical values

df

df.columns

x= df[['User ID', 'Gender', 'Age', 'EstimatedSalary']] 
y=df[['Purchased']]

x_train,x_test,y_train,y_test=train_test_split(x,y,test_size=0.25,random_state=29)

df["Gender"].replace({'Male':0,'Female':1},inplace=True)

df

model=LogisticRegression()
model.fit(x_train,y_train)

y_predict=model.predict(x_test)   #i.e. x ki test value pe prediction karega and y ki value predict krega

y_predict

model.score(x_train,y_train)

model.score(x,y)

model.score(x_test,y_test)

cm=confusion_matrix(y_test,y_predict)

cm

print(confusion_matrix.__doc__)

tn, fp, fn, tp = confusion_matrix(y_test,y_predict).ravel()

print(tn,fp,fn,tp)

a=accuracy_score(y_test,y_predict)      #all correct values ...by..   accuracy=tp/(tp+fp+tn+fn)
a

e=1-a
e      

precision_score(y_test,y_predict)

recall_score(y_test,y_predict)

accuracy=(tp+tn/(tp+fp+tn+fn))
accuracy

plt.figure(figsize=(5,4))
sns.heatmap(cm)
plt.title('Confusion Matrix')
plt.ylabel('Actual Values')
plt.xlabel('Predicted Values')
plt.show()
