Data Analytics I 

Create a Linear Regression Model using Python/R to predict home prices using Boston Housing Dataset (https://www.kaggle.com/c/boston-housing). 
The Boston Housing dataset contains information about various houses in Boston through different parameters. There are 506 samples and 14 feature variables in this dataset. 
The objective is to predict the value of prices of the house using the given features.


import pandas as pd 
import numpy as np 
from sklearn.model_selection import train_test_split   #for ml,algo models
from sklearn.linear_model import LinearRegression
from sklearn.metrics import mean_squared_error   #for showing errors
import matplotlib.pyplot as plt 
import seaborn as sns

df=pd.read_csv(r"C:\Users\DELL\Desktop\dsbdal\BostonHousing.csv")

df

df.columns

x = df[['crim', 'zn', 'indus', 'chas', 'nox', 'rm', 'age', 'dis', 'rad', 'tax',
       'ptratio', 'b', 'lstat', ]]
y = df['medv']

y

x_train, x_test, y_train ,y_test = train_test_split(x,y,test_size=0.25, random_state=42)

model=LinearRegression()
model.fit(x_train,y_train)

y_predict = model.predict(x_test)
print(x_test)

y_predict

model.score(x_train ,y_train)      #gives accuracy value below

model.score(x_test,y_test)

mean_squared_error(y_test , y_predict)  #takes diff of errors predicted and actual value ka diff leke uska square krke mean nikalta hai

np.sqrt(mean_squared_error(y_test , y_predict))
